# {{PROJECT_NAME}}

{{PURPOSE}}

## Status

This project was generated by Agent Builder {{TEMPLATE_VERSION}} on {{GENERATED_DATE}}.
It begins in the **Define** phase and has no agent runtime yet.

Selected coding-agent adapters: {{ADAPTER_LIST}}.

## Start here

1. Open the repository with one of the selected coding agents.
2. Read its root instruction file and `governance/operating-agreement.md`.
3. Complete the Define questions in `planning/open-questions.md`.
4. Ask the project author to approve the transition to Plan.

Do not select a runtime, add APIs, or implement agent behavior during Define.

## Structure

- `governance/` — human operating agreement and machine-readable state and decisions.
- `planning/` — evolving requirements, research, questions, and implementation plan.
- `contracts/` — provider-independent schemas for communication and integrations.
- `deployment/` — container policy; runtime-specific files are intentionally deferred.
- `src/{{PYTHON_PACKAGE}}/` — Python package placeholder for the approved Build phase.
- `tests/` — deterministic scaffold checks and, later, agent acceptance fixtures.

## Validate the scaffold

```sh
python3 -m unittest discover -s tests -v
python3 -m compileall -q src tests
```

## License

No license has been selected for this generated project. Choose one before public
distribution and record third-party license obligations as dependencies are added.
