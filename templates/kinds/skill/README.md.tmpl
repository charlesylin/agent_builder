# {{PROJECT_NAME}}

{{PURPOSE}}

## Status

This skill was generated by Agent Builder {{TEMPLATE_VERSION}} on {{GENERATED_DATE}}.
It begins in the **Define** phase. `SKILL.md` is a scaffold until Define and Plan are done.

Selected coding-agent adapters: {{ADAPTER_LIST}}.

## Start here

1. Open the repository with one of the selected coding agents.
2. Read its root instruction file and `governance/operating-agreement.md`.
3. Complete the Define questions in `planning/open-questions.md`.
4. Ask the project author to approve the transition to Plan.

Do not write the procedure in `SKILL.md` or add scripts during Define.

## Structure

- `SKILL.md` — what the host reads: front matter with `name` and `description`, then the procedure.
- `scripts/` — deterministic helpers the procedure runs; each has a test.
- `references/` — material loaded on demand, not on every activation.
- `evals/` — scenarios that check the skill behaves as written.
- `.claude-plugin/plugin.json` — manifest for installing this skill as a Claude plugin.
- `governance/` — operating agreement, phase state, and decisions.
- `planning/` — requirements, research, open questions, and the implementation plan.
- `tests/` — deterministic checks for the scaffold and the scripts.

## Install for local use

Claude Code and Codex both read a skill from a directory containing `SKILL.md`. Copy or link
this repository into `~/.claude/skills/{{PROJECT_SLUG}}` or `~/.agents/skills/{{PROJECT_SLUG}}`
to try it, or install it as a plugin once it leaves Define.

## Validate the scaffold

```sh
python3 -m unittest discover -s tests -v
```

## License

No license has been selected for this generated project. Choose one before distribution.
